# coding: utf-8

"""
    Talon.One API

    Use the Talon.One API to integrate with your application and to manage applications and campaigns:  - Use the operations in the [Integration API section](#integration-api) to integrate with our platform. - Use the operation in the [Management API section](#management-api) to manage applications and campaigns.  ## Determining the base URL of the endpoints  The API is available at the same hostname as your Campaign Manager deployment.  For example, if you access the Campaign Manager at `https://yourbaseurl.talon.one/`, the URL for the [updateCustomerSessionV2](https://docs.talon.one/integration-api#tag/Customer-sessions/operation/updateCustomerSessionV2) endpoint is `https://yourbaseurl.talon.one/v2/customer_sessions/{Id}`. 

    The version of the OpenAPI document: 
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Union
from typing_extensions import Annotated
from talon_one.models.experiment_segment_insight_variant import ExperimentSegmentInsightVariant
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ExperimentSegmentInsight(BaseModel):
    """
    ExperimentSegmentInsight
    """ # noqa: E501
    dimension: StrictStr = Field(description="The segmentation dimension used to group customers or purchases for analysis.")
    bucket: StrictStr = Field(description="The specific group within the segmentation dimension.")
    confidence: Union[Annotated[float, Field(le=100, strict=True, ge=95)], Annotated[int, Field(le=100, strict=True, ge=95)]] = Field(description="The raw (unadjusted) confidence score expressed as a percentage. Only segments with a confidence score greater than or equal to 95% are returned. ")
    winner_variant_id: StrictInt = Field(description="The ID of the variant that performed better in this segment.", alias="winnerVariantId")
    variants: List[ExperimentSegmentInsightVariant] = Field(description="Per-variant metric values for this segment.")
    __properties: ClassVar[List[str]] = ["dimension", "bucket", "confidence", "winnerVariantId", "variants"]

    @field_validator('dimension')
    def dimension_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['cart_value', 'item_count', 'customer_type']):
            raise ValueError("must be one of enum values ('cart_value', 'item_count', 'customer_type')")
        return value

    @field_validator('bucket')
    def bucket_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['low', 'medium', 'high', 'new', 'returning', 'loyal']):
            raise ValueError("must be one of enum values ('low', 'medium', 'high', 'new', 'returning', 'loyal')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ExperimentSegmentInsight from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in variants (list)
        _items = []
        if self.variants:
            for _item_variants in self.variants:
                if _item_variants:
                    _items.append(_item_variants.to_dict())
            _dict['variants'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ExperimentSegmentInsight from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "dimension": obj.get("dimension"),
            "bucket": obj.get("bucket"),
            "confidence": obj.get("confidence"),
            "winnerVariantId": obj.get("winnerVariantId"),
            "variants": [ExperimentSegmentInsightVariant.from_dict(_item) for _item in obj["variants"]] if obj.get("variants") is not None else None
        })
        return _obj


